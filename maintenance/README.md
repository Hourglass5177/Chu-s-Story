# 维护记录

cleanup-20260916 是本轮清理批次标识；实际执行时间以凭据 deleted_utc 为准。

- preserved.json：最终成果和必要证据的原位置、保留位置及SHA-256。
- frozen-manifest.json：逐项路径、文件清单、大小、修改时间、哈希和Git跟踪状态。
- receipts.jsonl：每项系统回收站元数据位置、数据位置、Shell可见性及内容校验结果。
- exceptions.json：没有回收的具体例外。
- 最终结果以完成后生成的验收报告为准；有清单不代表已经回收。

恢复时在Windows资源管理器的回收站，按原始位置找到目标并选择还原。不要直接改名或搬动 $I/$R 文件。回收站仍占用磁盘，本轮不清空。

tools/maintenance 下的脚本面向本次冻结清单，不是定期自动清理任务。不得直接复用旧清单清理重新生成的同名文件。
