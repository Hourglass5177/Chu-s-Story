# 项目清理与指南 CI 修复验收（2026-09-18）

## 已完成回收

本轮批次标识为 cleanup-20260916，实际执行日期为 2026-09-18。冻结清单最终包含 692 个互不重叠的目标、44,389 个文件、17,302,799,385 字节（16.11 GiB 逻辑大小）；按文件身份去重为 17,086,880,567 字节，不能把硬链接额外名称算作重复物理占用。

所有目标均核验了原路径消失、资源管理器回收站可见、原始路径和时间、$I 元数据、$R 数据、文件数与字节数，以及逐文件 SHA-256。实际凭据见 [回收清单](回收清单.csv)、[汇总](recycle-summary.json) 和 receipts.jsonl。只移动到 Windows 系统回收站，没有永久删除，也没有清空回收站；这减少工作目录体积，不释放回收站仍占用的磁盘空间。

回收范围包括旧构建及包体替换副本、AI 全工程快照、宣传片原录像/采集音频/中间输出、Remotion 安装依赖与媒体副本、旧审稿与测试输出、导入及着色器缓存、已汇总的阶段报告、三个过时工具/预览和重复字体。

## 保留与例外

- deliverables/ 保留最新公司署名 Windows ZIP、118 秒最终宣传片及 1920×1080 宣传图，三者 SHA-256 均与整理前相同。桌面提交包未改动。
- .git、玩家存档、原卡牌/角色、有效素材源稿、授权原媒体及许可证保留。v1/v2 仍有运行备用引用，v3 仍引用旧来源记录，因此没有整删版本目录。
- artifacts/media-20260909 仍为有效媒体工具/资料来源，保留；docs/verification 保留必要 AI 结果与失败复现、历史回收及署名证据。
- 宣传片父目录无法原生回收（0x80270021）；已将冻结清单拆为其 38 个子目标，逐个验证回收成功，只保留空父目录。未改用永久删除。
- 元数据曾短暂被占用，停止后修正共享读取并按凭据续跑；没有重复移动已完成目标。
- 清理前回收站已有 7 个孤立 $I（初始共 799 字节），本轮保留其元数据。这些旧记录的数据不可用不能算成本轮丢失；本轮新回收项均有对应 $R 且通过哈希核对。

原始录像已按确认决定回收。tools/promotion-video 保留用户定稿字幕、时间线、脚本和锁文件，但不能直接重渲染原片；改镜头需重新录制。

## 指南 CI 修复

指南正文和结构配图源文件统一按文本换行计算摘要，LF 与 CRLF 得到同一哈希；不删除正文空白，不忽略真实内容修改。同步更新生成目录与 GUT 校验。新增 Python 回归覆盖两类源文件换行等价、真实内容变化及当前生成物同步，并接入 Beta 入口。原有 --check 校验保持启用。

已回收阶段报告的名称引用检查：保留的 Git 跟踪文本文件中为 0 条。历史凭据中的原路径用于恢复，不属于失效运行引用。

## 当前验证

本轮完整 Beta 通过：2项指南Python回归、89个GUT脚本中的767项测试/42,545条断言、真实指针前端会话及2/3/6人三种策略共9局模拟。导入及着色器缓存先进入回收站，再从空导入缓存执行重新导入；使用 -SkipCleanImport 保留编辑器/UID缓存，未再次永久删除 .godot。

临时 Windows Release 导出通过，实际Release程序启动/关闭、15项任务/90套角色、296张运行纹理、三故事9幅拼图、11项媒体解码/参考数据、指南JSON和CREPE模型加载/析构均通过，无麦克风采集。导出程序实际截图已查看，各主题画面可见；这是资源完整性检查，不是新一轮动作/手感或用户认可。模型及媒体记录见 resource-model-report.json，程序哈希与退出结果见 release-check-summary.json。

旧Release验证工具使用了Release模板不支持的 --script、旧四幕排序断言和不适合导出资源的目录枚举。已改用临时导出旁 override.cfg 加载独立检查场景，结束即移走配置；按当前三故事九幅图检查，并通过 ResourceLoader.list_directory 枚举运行资源。首次工具检查失败记录保留，修正后的复验通过，没有修改游戏玩法来迁就测试。

保留的历史参赛ZIP没有附带四个Visual C++运行库DLL，可能依赖目标机已有运行库。本轮不修改该ZIP；临时Release已从匹配的Visual Studio VC145 Redist复制运行库并记录哈希，未复制系统DLL。临时Release及本轮验证文件放在 artifacts/cleanup-validation；不替换桌面提交包。

回收后的导入缓存及 artifacts/verify 等由本轮验证重新生成，不属于遗漏旧垃圾。历史AI矩阵仍为772/4968局，音游听审、董永v8及部分素材用户认可仍待完成。

## 恢复方法

在 Windows 资源管理器打开回收站，依据回收清单的原始位置选择“还原”。不要手动改名 $I/$R。此次维护脚本绑定历史冻结清单，不能再次拿旧清单清理同名新生成文件。
