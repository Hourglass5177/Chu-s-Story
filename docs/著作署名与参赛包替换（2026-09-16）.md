# 著作署名与参赛包替换

用户明确公司名为“楚行九州有限公司”，不使用旧提交 `9fdaab6` 中的“九洲”。`main_menu.tscn` 恢复公司标题，保留原小组十三位成员：王诗雨、王妍舒、高致远、李丁捷、吴佳琪、沈妙莹、胡冰倩、白叔瑜、张芯瑜、卢桢栋、田伊可、袁子伊、杨恒杰。数字版策划、程序与杨恒杰、杨子暄的美术署名保持。Windows EXE 的 CompanyName 同步为“楚行九州有限公司”。

本轮仅修改署名与导出公司字段，没有更改游戏规则。1280×720 实际著作页截图：`artifacts/credits-submission/credits-1280.png`。使用 Godot 4.7.2 导出 Windows Release，执行独立用户目录下的启动及自动退出检查；未重跑完整 Beta，不把启动检查表述为完整玩法、媒体或模型验收。

替换目标为 `F:/Desktop/楚物志.7z` 内的 `楚物志_楚行九州_Windows参赛版.zip`。操作工具 `tools/update_submission_archive.py` 先备份原 7z，再在工作副本内更新一个成员；通过新 ZIP CRC、7z 完整性、解压后 SHA-256 和非目标成员时间/属性/CRC 对比后，才替换桌面文件。其他五份文件必须逐字节不变，且保留原成员名称和元数据。

备份：`artifacts/credits-submission/original-楚物志.7z`。
实际完成结果与哈希以 `artifacts/credits-submission/archive-update-report.json` 为准；报告不存在则表示替换尚未完成。

未提交或推送 Git。
