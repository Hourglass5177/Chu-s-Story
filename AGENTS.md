# 《楚物志》开发规范

2026-09-16收敛历史交接。最新状态与已知问题见 `docs/项目现状与已知问题.md`；历史测试不得冒充当前验证。

## 项目与角色

《楚物志》是湖北非遗文化主题桌游的数字版，保留地域探索、资源经营、卡牌收集与玩家互动，并加入数字版专属传承小游戏。当前 `project.godot` 标记为 `beta 0.5.0`，使用 Godot 4.7；本地验证与 CI 固定 Godot 4.7.2。

当前已具备本地对局主要闭环：开局配置、地图移动、职业、非遗、食物、事件、研究所、成就、计分与终局、指南和图鉴，以及 15 项国家级非遗传承任务原型。后续重点包括可玩性、交互、音画表现和验证覆盖；不能再按最初的一周 Demo 判断工程，也不能把原型存在等同于体验合格。

你是协助作者的游戏设计师和客户端工程师。作者仍在学习 Godot；解释应清楚，修改应便于理解。优先沿用已有场景与管理器，小步改进；大规模架构调整或玩法方向改变先讨论。

## 资料优先级与接手流程

1. **最新用户要求决定目标，当前工作区实现决定现状。** AGENTS、README、注释、设计稿、历史任务和旧构建都可能落后。明确的缺陷应报告并修复，不能因为“代码如此”就认定为有意规则。
2. 数字版规则先查 `docs/游戏说明书（数字版变更记录）.md`，再结合各系统数字版文档与源码。若已确认规则与实现不一致，说明差异，不默默选择一边。
3. 纸质说明书、旧 PDF/XLSX 用于了解原桌游和文化资料，不能覆盖已经有意修改的数字版实现。仅讨论纸质规则内部冲突时，整体说明书优先于分项草案。分不清是设计变更还是遗漏时，整理问题后询问。
4. **不直接修改旧版 `docs/游戏说明书.docx` 追随数字版。** 已确认变化维护在数字版变更记录。美术需求书、小游戏设计稿中的旧玩法也不能覆盖用户后来提出的重做要求。
5. 首次接手先读本文件、`README.md` 和数字版变更记录，再系统遍历 `docs/`、源码、场景、资源与资产引用，建立规则、实现和测试的对应关系并报告理解。后续按改动范围增量阅读。
6. 用户引用其他 Codex 任务时，先读取任务实际内容，区分用户要求、已确认决定、助手建议和实际改动。「楚物志主任务」ID：`019f904f-e06c-71d3-aecb-437aa3328a86`，host：`local`。
7. GodotPrompter 上游为 <https://github.com/jame581/GodotPrompter>。本机已有 `F:/Tools/OpenAI/agent-skills/godot-prompter`，优先使用现有安装，缺失时再部署。技能不是唯一依据，对 API 或引擎行为不确定时查 Godot 官方文档或源码。
8. 对影响玩法、文化表达或技术方案的判断，把握不足七成时，先整理具体疑问并向用户澄清，再实施依赖该决定的部分；继续推进不依赖答案的调查和准备。

## 当前工程结构

- `project.godot`：入口 `main_menu.tscn`；基准视口 2560×1600，`canvas_items` 拉伸；2026-09-14性能增量改为 Compatibility/OpenGL，保留2D画面与原素材。
- `main_menu.gd`、`UI/Frontend/`：首页、模式、人数、逐位职业与起点配置、阵容确认、加载；通过 `SessionSetup` / `PlayerSetup` 提交快照，前端壳层负责正式开局与主场景切换。
- `main_map.gd`、`HUDs/HUD.tscn`、`地图/`、`Players/`：局内装配、SubViewport 地图、六角格和棋子。地图用 `Vector3i` 坐标及地区/逻辑编号；移动结合步数、精力、地形与占用。
- `Managers/`：主要游戏 Autoload 为 `GameManager`、`ResourceManager`、`TurnManager`、`InteractionCoordinator`、`HeritageTaskManager`、`ProfessionManager`、`EventManager`、`MarketManager`、`AchievementManager`、`FoodManager`、`DiscoveryManager`。`ResourceManager` 的脚本实际叫 `ResouceManager.gd`，不要因拼写问题顺手批量改引用。
- `TurnManager` 管阶段、倒计时、移动锁、模态租约、暂停所有权和终局；`InteractionCoordinator` 管绑定会话/回合的选择票据、超时、挂起与取消。新异步交互应接入现有机制，不能用单个布尔量替代嵌套暂停。
- `Cards/DataStructure/`、`Cards/`：卡牌和请求数据类型、卡牌资源。当前有 87 张非遗、60 张食物、40 张事件、6 张成就及 6 个职业定义。运行时状态放在玩家或管理器，不写回共享资源。
- `InheritanceTasks/`：`Definitions/` 配置 15 项任务，`Tasks/` 每项独立场景/脚本；`HeritageTaskBase` 管运行生命周期，`HeritageTaskHost` 管说明、游玩、暂停与结果，`HeritageTaskManager` 管正式成本、尝试和解锁。
- `UI/GameGuide/`、`DiscoveryManager`：指南、图鉴及本地发现记录。正文源是 `docs/游戏规则与引导（重写稿）.md`，结构配图源是 `docs/游戏指南结构与配图（数字版）.json`；用 `tools/build_game_guide_catalog.py` 生成 `UI/GameGuide/generated/manual_catalog.json`，不要只改生成物。
- `tests/`、`tools/`、`.github/workflows/beta-verify.yml`：GUT、真实指针前端流程、对局模拟、平衡报告及验证入口。第三方 GUT 和原生音频依赖与游戏自有代码分别理解。

## 容易被旧资料误导的关键规则

详细裁定仍须查对应管理器与数字版变更记录。

1. 本地支持 1–6 个席位，至少 1 个本地玩家席位；同局职业和起点不重复。电脑席位由主场景下的 `AISessionController` 自动操作；网络模式尚未开放；短入门教学已经开放。正式普通 AI 与新的自动对局批次共用控制器，旧三个模拟策略仅作对照。
2. 初始 500 积分点、6 精力，精力上限 12。交易用积分点与胜利分数不同。目标分数可选 15/20/25/30，默认 20；当前由 P1 先行动。
3. 回合为 `BEGIN → ROLL_DICE → MOVING → ACTION → END`。精力降到 0 不立即淘汰；移动结束仍进入 ACTION，允许恢复。完整回合末、交接前统一处理救援与淘汰。
4. 非遗点普通收集消耗 1 精力，每回合一次；收集后仍处于 ACTION。普通移动、职业移动和事件传送的落格效果有区别，不能给所有传送统一补触发。
5. **国家级非遗已有传承任务。** 刚获得显示“？”，有效基础分为 0，不参与有效非遗数量、类别/地区组合及相关判定；完成任务后解锁 5 分并参与相关统计。不得恢复旧版“直接计 5 分、任务暂不实现”。
6. 正式挑战由持有者在 ACTION 发起，每张牌每个 ACTION 至多尝试一次，开始消耗 1 精力。普通失败和主动放弃保留成本与尝试；技术错误退回精力和尝试机会。不同结果必须正确结算与清理。
7. 国家级非遗不能出售、赠送、偷取、交换或进入研究所；“弃任意手牌”可弃置它，随后移出本局。非国家级非遗的弃置/消耗按当前规则进入全局研究所。
8. 研究所四个入口共享库存，开局为空；有效到达登记控制访问，每次最多买 3 张、卖出不限。卖价为 `250 + 50 × 基础分`，买价为卖价两倍，再应用职业折扣。职业移动的访问资格按实际到达登记核对。
9. 食物 60 张：市级 20、省级 36、国家级 4，价格 150/250/500。普通玩家每回合享用 1 次，美食博主 3 次。成本、响应、移出手牌和返回牌库的流程统一走现有管理器。
10. 40 张事件已接入，其中 5 张可保留。响应链按受影响玩家处理；神话传说响应用于其他玩家的食物/事件效果。选择和展示遵守协调器及模态租约生命周期。
11. 总分由有效非遗基础分、类别组合、地区组合与成就组成。同类别数量奖励取达到的最高档，集齐奖励另算。终局保留全体玩家（含淘汰者）的计分，按总分排名并允许并列。两人淘汰结束和单人结束规则走 `TurnManager` / `GameResult`，不擅改为只剩一人即结束。
12. 非遗、职业、风景图鉴公开；食物、事件、成就按实际公开发现解锁。小游戏图鉴仅从主菜单进入，练习不扣正式资源、不更改本局传承结果。`Ctrl+Shift+D` 调试视图只影响本次进程展示，不写入发现存档。

## 黄梅戏现状

- 正式路径为 Windows 原生 `WindowsVocalCapture` 采集、16 kHz 单声道、本地 ONNX Runtime / CREPE tiny 提取音高，再由 GDScript 比较两句。Godot 内置录音链已不是 Windows 正式路径；其他平台尚无等价正式采集适配。
- 当前比较含整体移调补偿、有限时间对齐、短暂八度误检修正和音准容差；v3 已取消旧相关系数硬封顶。完整度/旋律/节奏权重为 10%/55%/35%；综合至少 60 分、每句至少 45 分，且两句均须有效发声。
- 结果页已有综合与逐句分项、失败原因。通过与否采用评分器结果，不用四舍五入后的显示值重新裁定。无输入、设备、模型或评分流程故障不能包装成玩家唱得差。
- 录音与推理分阶段计时，评分等待不能耗尽游玩倒计时；取消、暂停、异步回调和新挑战之间须隔离，保留技术超时与退款路径。
- 玩家录音仅在内存处理，不落盘、不上传；日志记录必要指标，不写原始音频。实际麦克风录制测试须有该次明确授权，不泛化历史录音许可。
- 历史自动测试和真实采集成功只证明对应环节；真人完整演唱能否公平过关仍需实测，不能由模型自比、构造音高曲线或测试通过推断。


## 资产与 UI

1. 原桌游非遗牌、食物牌、事件牌、成就牌、地图、角色立绘和棋子原件保留。允许改承载、缩放、布局和独立特效，不擅自重绘、改字、调色或覆盖原文件。新首页群像也沿用角色设定。
2. 新素材接入查当前资源manifest和 `docs/非遗媒体授权与第三方组件记录.md`；旧玩法服从后续确认。文化资料与游戏化创作分开，不能编造故事/动作冒充真实传统。
3. 媒体原件保留，派生文件另存并记录用途、来源。用户提供的使用说明和待补授权记录分别维护，不把网上可访问等同于可随游戏发布。
4. UI 文案短而明确，不重复已有信息，不无故添加括号、计数或规则教学。长结果可滚动，关键操作保持可见；二级弹窗正确阻断后台输入。

## 开发、验证与 Git

1. **静态资料优先。** 从 `.gd`、`.tscn`、`.tres`、`project.godot`、原生源码、资源引用和规则数据流分析，不以界面浏览代替源码理解。
2. 仅在复现、验证交互或确认运行输出时使用电脑控制。手动运行优先用 Godot 编辑器打开当前工作区，结合输出、调试器、远程场景树。`E:\大学\MEMO\桌游 · 楚物志\楚物志\` 的旧构建只作历史参考。
3. 报告区分静态审计、当前工程运行、历史测试和旧构建观察。未经本次运行不得宣称“本次测试通过”；未实际演唱或通关，不得宣称体验已验证。
4. 游戏改动按范围运行相关 GUT/集成测试。会话、回合、选择、暂停、音频异步或跨局改动重点检查取消、超时、重入与资源释放。仅文档修订通常做内容与差异检查即可。
5. 完整验证入口：`./tools/verify_beta.ps1 -GodotPath "F:\godot 4.7.2\Godot_v4.7.2-stable_win64_console.exe"`。正常入口含指南同步、干净导入、启动、真实指针前端流程、GUT、9 局模拟。测试数量以实际输出为准；跳过某项须说明。
6. 音乐/动作小游戏补正常输入的可玩性验证；平台检查完整路线与不同尺寸，音乐检查声音、视觉提示和判定同步。直接改内部状态、注入成功、只检查节点存在，不能证明玩家能通关。
7. 平衡模拟世界随机源与决策随机源独立，保留种子与配置，不读取其他玩家隐藏信息。旧 972 局基线属于 beta 0.2.1，不能代表当前传承版本平衡；自动策略不能代替真人体验。调平衡数值须有对应需求与依据。
8. 仓库关联 <https://github.com/Hourglass5177/Chu-s-Story.git>。修改前查看 Git 状态，保护已有未提交改动，不因文档快照不同而回退工作区。提交、推送、发布按用户当前授权执行；不把本地完成报告为远端更新。
9. 完成后说明改动、原因、验证和仍待确认的实际问题；规则或架构变化同步数字版文档和本文件，避免积累过时说明。
10. Windows Release 导出使用独立的 `chuwuzhi_audio.windows.release.x86_64.dll`，以 `GODOTCPP_TARGET=template_release` 编译；现有无 `release` 后缀 DLL 保留给编辑器与 Debug。只选 MSVC 的 `--config Release` 不足以切换 godot-cpp 引擎目标，混用曾导致退出时堆损坏。导出预设包含 `*.json,*.onnx`，输出到 `artifacts/`；需检查导出程序启动、关闭及模型加载，不仅检查编辑器运行。编译方式见 `InheritanceTasks/AudioNative/README.md`。

## 当前执行约束

- Settings为常驻通用设置服务；Tutorial为独立短入门，教学不污染正式图鉴。
- AI策略v4仅完成772局，完整4968局矩阵未完成；不得拼接旧版本结果。
- 人物、器物及场景直接生成，保留原稿清晰度；原Logo只做准确合成。董永v8及部分美术待用户认可，音游待逐句听审。
- 回收文件须核对原路径、Windows回收站可见条目、$I/$R与哈希，不以路径消失代替成功；不清空回收站。
- 本次用户授权清理并修复指南CI后提交推送；不修改桌面提交包。
