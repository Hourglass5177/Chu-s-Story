# 《楚物志》主体声音设计与接入记录

## 最新：第五版区分成就与结算

用户指出成就与结算几乎相同，要求结算简化、成就更丰富华丽。成就现为1.90秒古筝五音上行，起点0/140/290/470/750ms，尾端以较轻的低根音与五度支撑高音落点；加入低电平33/59ms早期反射，保留柔起音，不叠锣。为扩展音域，仅成就使用邻近采样的有限变调（不超过4半音）。结算改为0.65秒原速低弦单音，无和弦、音阶、叠锣和额外反射。

只修改achievement/result两项，其余17个WAV逐字节保留。试听页将这两项置顶并提供第四版对照；旧文件与构建脚本归档 `artifacts/board-audio/pre-fanfare-v4/`。清单标记 `achievement-result-v5`，音量/时长数据见 `fanfare-v5-levels.json`。本轮艺术方向仍待用户听审。

第五版实际GUI音频与触发检查35项通过，报告 `artifacts/board-audio/fanfare-v5-verification.json`、总线捕获 `fanfare-v5-runtime-preview.wav`、日志 `fanfare-v5-runtime.log`。19个源文件与导入缓存哈希一致。运行日志出现并行设置面板 `UI/Settings/settings_panel.gd:291` 的锚点/尺寸警告，未修改该面板；无音效检查失败，未重跑完整Beta。

## 第四版历史：高频交互使用木鱼轻点

用户继续反馈突兀，要求确认、返回等高频提示采用轻声中国打击乐音色。源码中的所有普通按钮/页签复用 confirm，返回/取消/关闭复用 back；当前直接替换对应资源，响应也去掉弦音。确认/响应140ms，返回160ms，采用原速布头槌木鱼的单点短音，用滤波明暗和音量区分；确认稍清，返回更暗更轻。无效操作、倒计时和技术提示同步替换旧硬塑材质采样。

本次只变化6个音效，其余13个逐字节一致，BGM、小游戏、触发和限频机制不变。来源为 jonopodmore 的 CC0 木鱼录音（原物来自日本高野山），来源页、下载与处理记录在 `Audio/Source/chinese-instruments/SOURCES.md`。试听页新增连续8次确认/返回与第三版对照，默认应用游戏音量。前版资源和构建脚本保存在 `artifacts/board-audio/pre-percussion-v3/`。不将音量数据或音频输出正常当作用户听感验收。

本版实际 GUI 音频输出与触发检查35项通过，结果 `artifacts/board-audio/percussion-v4-verification.json`、日志 `percussion-v4-runtime.log`、总线捕获 `percussion-v4-runtime-preview.wav`。19个文件清单与导入源哈希一致。确认/返回在总线捕获点峰值分别约0.042/0.026，比第三版约0.086/0.084更轻；未重跑完整Beta。用户需重新运行已缓存旧资源的游戏，浏览器需刷新到第四版；本轮未停止其他实例。

## 第三版历史：轻声修订

用户反馈第二版不自然、有假感、过重突兀。当前同样 13 个提示改用原速采样的一至两音，撤掉叠锣、变调和密集乐句；返回为柔和单音收束。古筝起音放柔至 120ms，尾部用 220ms 渐收，温和削减低频冲击及尖锐高频；木鱼式材质敲击保留原速并放轻。固定输出增益替代逐文件峰值归一，避免柔化后再次被放大。六个实物拟音、BGM、触发规则和小游戏音频保持。

当前 13 个文件峰值较第二版降低约 4.9–8.9dB，6 个拟音逐字节一致。试听页默认应用游戏中单音效和总线音量，保留切换原始响度、新旧对照和 BGM 混听。此数值检查不能证明听感自然；仍以用户试听为准。第二版源构建脚本和音频保留在 `artifacts/board-audio/pre-soft-v2/`。

第三版 Godot GUI 实际音频输出与触发检查 35 项通过，报告 `artifacts/board-audio/soft-v3-verification.json`，日志 `soft-v3-runtime.log`，总线捕获 `soft-v3-runtime-preview.wav`。19 个清单哈希及 13 个修改后导入缓存来源哈希已核对；未重跑完整 Beta，未停止既有游戏、编辑器或其他测试实例。已运行实例可能仍缓存旧音效，下次重新运行游戏载入新版。

## 第二版历史：大多数提示音改用中国乐器方向

用户明确“turn、action、event、achievement 等等”指整体提示音重做，并否定旧返回音。当前重做 13/19，包含确认、返回、无效操作和全部状态提示；其余骰子滚动/落定、抽牌/翻牌、交易、落子共 6 个物件拟音保留。

- 确认/行动：古筝清拨与短止音；返回：古筝短下行收束，撤掉被否定的旧 back_001。
- 回合/事件/响应/恢复/淘汰：古筝双音、低弦、上行或收束，时长与阻尼分开设计。
- 成就/结算：古筝五声短乐句，轻叠中国锣实录，不使用大音量锣鼓长奏。
- 无效操作/倒计时/技术提示：木鱼式干敲击，保持语义差异；采样原物为硬塑材质，不称传统木鱼实录。

真实古筝/锣采样与木鱼式材质采样均核对 CC0，完整来源与处理说明在 `Audio/Source/chinese-instruments/SOURCES.md`。19 个运行时文件名、既有触发与 BGM 不变。试听页新增 13 个新旧对照并用音频哈希参数避免旧缓存，旧版保留。下面实施与验证章节保留第一版历史；不将旧版 35 项结果直接当成本轮音色听审。

本轮重新运行 Godot GUI/WASAPI 检查 35 项全部通过，日志 `artifacts/board-audio/chinese-runtime.log` 无脚本错误，结果 `chinese-verification.json`，音效总线捕获 `chinese-runtime-preview.wav`。逐文件比较确认恰好 13 个音效变化、6 个拟音保持字节一致；19 个 WAV 均有非零波形、留有峰值余量且尾帧归零。一次并行编辑器导入遇到既有原生扩展临时 DLL 占用，未关闭用户编辑器或改动扩展；随后正式运行使用原扩展正常完成。未把程序输出检查作为用户听感认可，未重跑完整 Beta。

2026-09-15。用户随后明确要求实装音效。BGM 保持上一轮实现；主体 19 个短音效现已生成/整理并接入，下面清单保留为设计依据，实际状态以本节及“主体音效接入”小节为准。

## 已实现：用户提供的 BGM

`Audio/board_music.gd` 作为 `BoardMusic` Autoload，由 `main_menu.gd`、`main_map.gd` 请求开始。整个进程只保留一个播放器，切换菜单、地图、教程与暂停不重头播放。原曲在音频流内部循环，不依赖每帧判断或 `finished` 后重播。

独立 `BoardMusic → Master` 总线默认 -10 dB，启动/恢复淡入 0.65 秒。原 MP3 峰值约 0 dBFS，平均电平约 -14.3 dBFS（FFmpeg volumedetect，不是 LUFS），预留音效混音余量。文件没有转码或替换，详情见 `Audio/Music/SOURCE.md`。

`HeritageTaskHost` 入树时占用主体音乐；准备、教学、演唱、暂停和结果期间保留 BGM 播放位置，退出 Host 后续播。占用跟随节点生命期，多个 Host 必须全部退出才恢复，技术失败和切场景不会留下永久静音。小游戏自身音量、原声、节拍与评分逻辑保持原实现；不采集麦克风。无窗口测试与 AI 批次不启动音乐解码。

主体目前没有全局声音设置页面。本次保留独立总线作为后续音乐/音效音量入口；小游戏音量旋钮仍仅作用于小游戏。

## 接入前源码盘点（历史基线）

- 主菜单 `FrontendUIPreferences.ui_feedback_requested` 已有 confirm/invalid 意图，目前没有对应声音消费者，仍需接入。
- `UI/Shared/board_dice_presenter.gd` 已有掷骰视觉演出，缺少滚动与落定声音。
- HUD、背包、商店、研究所、计分、事件弹窗与棋子移动目前缺少成体系的声音反馈。
- `Tutorial/tutorial_controller.gd` 已复用 `InheritanceTasks/Audio/rhythm-v2/confirm.wav` 作为低音量教程确认音。
- 小游戏已有操作提示、命中/失误、故事旁白、连续表演原声与教学音频，保留，不作为主体缺失项重复制作。

## 音效清单（1 最高；本轮不扩展 3 以下的低优先级装饰声音）

| 优先级 | 事件 | 建议声音与时长 | 接入位置/约束 |
|---|---|---|---|
| 1 | 确认、返回、拒绝操作 | 轻木扣 60–120ms；返回稍低；拒绝短闷双音 150ms | 前端 feedback 信号与共用按钮；一次输入只响一次，鼠标移过默认不响 |
| 1 | 掷骰与落定 | 木骰滚动约 1 秒，末次碰撞独立短音 | board_dice_presenter；跟视觉落定同步；不再次随机骰点 |
| 1 | 抽牌、获得非遗、打开牌面 | 纸张抽动/轻翻 100–250ms，获得时加很轻的亮音 | HUD 列表/抽牌完成信号；批量卡牌不逐张密集响 |
| 1 | 购买、出售、支付成功 | 短钱币碰响 150–300ms | 商店/研究所交易成功后触发；按钮点击与交易成功不叠两次大音 |
| 1 | 轮到真人、进入行动阶段 | 克制的木磬/两音提示 250–450ms | TurnManager 阶段信号；电脑加速时抑制连续阶段提示 |
| 1 | 真人倒计时临近结束 | 低强度短提醒 100–180ms | 仅阈值提醒；暂停/模态挂起时不响，不逐秒催促 |
| 2 | 棋子移动与落格 | 轻木棋子落盘 60–100ms | 移动动画落脚/最终到达；连续移动限频，职业传送单独轻掠 |
| 2 | 食物享用、精力恢复 | 轻碗碟声＋柔和上行短音 200–400ms | FoodManager 效果真正成功后；不采用夸张咀嚼声 |
| 2 | 事件揭晓、响应与取消 | 翻纸＋短提示，拒绝响应较低 | EventManager / InteractionCoordinator 正式结果；私密事件不暴露额外信息 |
| 2 | 成就达成、终局结算 | 短钟磬和弦 0.6–1.2 秒 | AchievementManager、结果面板首次显示；并列胜利不播放失败音 |
| 2 | 救援、淘汰、技术失败 | 柔和但可区分的下行/中性提示 250–500ms | 正式结果事件；技术问题不使用“玩家失败”的声音 |
| 3 | 页签、卷轴/弹窗打开关闭 | 极轻翻页/布纸声 80–180ms | 共用组件，选择性接入；频繁操作不堆积 |

## 素材与制作方向

主体的宣纸、木质框、地图、卡牌对应纸张摩擦、木块碰撞、少量温暖金属余音。原角色与原画不变；局部使用像素字体不意味着全游戏都要改成 8-bit 蜂鸣声。

已核对发布者页面：

- [Kenney Interface Sounds](https://kenney.nl/assets/interface-sounds)：100 个界面音效，CC0；优先筛选确认/返回类。
- [Kenney Casino Audio](https://kenney.nl/assets/casino-audio)：50 个卡牌、骰子、筹码拟音，CC0；优先筛选纸牌与骰子，声音用途改为本游戏的桌游物件。
- 钟磬式短提醒、恢复、成就可自行合成：正弦/三角波加少量泛音、短冲击包络与渐消尾音；不声称是湖北传统乐器的真实录音。

素材采用时保留包内许可、原文件名、来源 URL 与哈希。本轮已下载两个 Kenney 包、选取 9 个片段、加入 10 个自行合成的提示，生成 19 个 WAV；保留短起落包络与峰值余量，来源见 `Audio/SFX/SOURCES.md`。实际输出验证和用户听感验收分开记录。

## 实施顺序与验收

接入顺序为“按钮确认/拒绝 → 骰子 → 抽牌 → 交易 → 回合提醒”，再补优先级 2。短音效预制并缓存，少量播放器复用；不在玩家操作帧生成 PCM，不逐次新建播放器。声音随机变化使用独立 RandomNumberGenerator，不能消耗游戏规则的随机序列。限制同类并发和短间隔连发，AI 3 倍速下仍需检查听感。

音乐、主体 SFX、小游戏分别管理；先留混音余量，不靠提高所有声音响度制造反馈。键盘、鼠标、手柄共用结果信号；声音只辅助视觉，不成为唯一提示。验收包括真实输出、切场景、暂停、循环边界、小游戏让声与恢复；耳机/扬声器听感与长时间疲劳需另作听审。

## 本轮使用的技能

- 现有 `godot-prompter:audio-system`：持久播放器、音频流循环、独立总线。
- 新安装 [audio-design](https://github.com/gamedev-skills/awesome-gamedev-agent-skills/tree/main/skills/disciplines/audio-design)：音量余量、重要声音让声、重复音效变体。
- 新安装 [creating-godot-procedural-audio](https://github.com/abagames/agentic-gamedev-skills/tree/main/.agents/skills/creating-godot-procedural-audio)：语义清晰、克制、材质对应、预生成缓存。

技能里的多层动态配乐和“仅合成音频”是通用建议；本项目按用户要求使用所给原曲循环，并允许免费授权素材，不照搬不适用的部分。

## 本轮验证结果

- Godot 4.7.2 导入通过，未清除共享导入缓存。
- `tools/verify_board_music.tscn`：GUI/WASAPI 实际运行 11 项检查全部通过，报告 `artifacts/board-audio/verification.json`。包括主菜单自动启动、总线非零 PCM、暂停继续播放、重复启动不重置、原曲曲尾循环、小游戏准备页静音、嵌套占用、返回续播、菜单→地图→菜单与场景销毁清理。
- 在原曲结束前 0.8 秒开始检验，2 秒后播放位置为约 1.101 秒，验证流已循环；没有快进整首音乐或变更播放速度。`artifacts/board-audio/loop-boundary.wav` 是本进程音乐总线曲尾截取，未录麦克风或系统声音；音频效果捕获位于总线推子之前，文件不代表 -10 dB 后的最终输出响度。
- 现有小游戏电视/像素 Host 回归：18 项测试、201 项断言通过，日志 `artifacts/board-audio/host-regression.log`。
- BGM 轮没有重跑完整 Beta；没有进行耳机/扬声器人工听审，也不将非零波形或测试通过等同于混音听感合格。保留原曲头尾，未另制无缝音乐版本。

## 主体音效接入（后续实施）

`Audio/board_sfx.gd` 为独立 BoardSfx Autoload；由正式菜单和地图开启作用范围，在场景释放时清理。19 个 WAV 由 `tools/build_board_sfx.py` 构建，4 个播放器复用、独立 BoardSFX 总线、每类冷却、同帧按语义优先级合并、池满仅允许高优先级提示替换低优先级声音。音效和 BGM 的音量、播放位置互不覆盖。

- 菜单及主体弹窗按钮通过 pressed 信号统一响应鼠标/键盘/手柄；返回/取消/关闭为独立短音。前端已有 ui_sound_enabled 偏好继续有效。鼠标经过不响。
- 非遗、职业、成就、计分和背包详情打开时播放轻翻页音；快速销毁控件的延迟绑定采用 WeakRef，防止悬空回调。
- 骰子由 BoardDicePresenter 的实际滚动/落定时刻驱动；暂停时冻结滚动声音，取消时停止，电脑加速提前落定会截断滚动尾音。
- 卡牌通过 card_hand_visual_requested 获得事件触发；交易通过 food_purchased/work_completed/transaction_completed 成功信号触发；同帧交易音优先于附带的抽牌音。
- 食物只在 success 且 effect_applied 时播放恢复音；未生效不冒充恢复。妙手回春按正式恢复原因触发；事件揭示与响应、成就、淘汰与终局分别采用独立提示。
- 棋子声音跟随 movement_completed 的最终到达，不每帧或每格密集播放。回合/行动提醒仅真人；操作倒计时只在 MOVING/ACTION 的 5 秒阈值提醒一次，模态/暂停/教学无催促声。
- 电脑事件额外降低 5 dB 并至少间隔 450ms，省略回合、行动、倒计时提醒。
- 小游戏 Host 内主体音效全部屏蔽并停止尾音；正式传承技术错误在返回主体后播放中性短提示，不盖住小游戏音频。保留小游戏原声/输入提示与教程既有确认音。

这里的错误音只覆盖已有明确拒绝反馈/食物失败结果，未使用文案关键词推测业务成功或失败。禁用按钮保持不触发；鼠标直接点击禁用状态不会额外发声。

试听页面：`artifacts/board-audio/sfx-review.html`；实机验证入口：`tools/verify_board_sfx.tscn`。尚未新增全局音量设置页。

### 音效实施验证

- 最终 GUI/WASAPI 验证 35 项通过，`artifacts/board-audio/sfx-runtime-final.log`、`sfx-verification.json`。19 个短音效均有非零 PCM 输出；同时检查了正式菜单真实指针确认、无效操作优先级、前端偏好、规则 RNG 隔离、真实购买成功/失败、食物结果信号、电脑合并限频、暂停/倒计时、正式骰子演出与小游戏进出。
- `sfx-runtime-preview.wav` 是 19 个音效依次播放时本进程 BoardSFX 总线的捕获串联，插入短静音便于试听；位于总线推子之前，未包含 BGM、系统声或麦克风。食物检查使用构造的结果信号，不能冒充完整食物交互实测。
- 前端、研究所界面、事件弹窗生命周期、小游戏 Host 回归 51 项/591 断言通过，最终日志 `sfx-regression-final.log` 无脚本错误。首轮虽断言通过，但出现了延迟绑定悬空引用错误，已修复后重跑；不采用首轮日志作为通过证据。
- 指针首轮发现页面隐藏导致确认音漏播，随后移除错误的可见性后验过滤；最终指针确认只响一次。电脑同帧交易优先于附带抽牌，限频时间在实际播放后记账。
- 未新增人工耳机/扬声器听审；自动音频输出检查不判断听觉疲劳、文化适配或主观悦耳程度。该任务未独立重跑完整 Beta，不借用另一个开发任务正在运行的结果。
